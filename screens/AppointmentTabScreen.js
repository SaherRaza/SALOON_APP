import { View, StyleSheet, Text } from 'react-native';
import React from 'react';

function AppointmentTabScreen({navigation}) {
    return (
        <View>
            <Text>profile</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    
})

export default AppointmentTabScreen;